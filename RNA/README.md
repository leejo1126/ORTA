# RNA module

**Status:** planned
**One-liner:** RNA readouts (nascent transcription / FISH) of the E–P system.

Planned sibling module under the ORTA umbrella. When activated, copy `_templates/module/` structure,
fill in `src/`, `config/`, `workflow/`, `env/`, and start the first run under `experiments/`. Register
in `../OVERVIEW.md` and add `../lab-notebook/rna.md`. Marker biology / methods generalize from `../wiki/`.
