# agents/knowledge/ — moved

The agent knowledge cards now live in the **unified project wiki** at [`../../wiki/`](../../wiki/)
(domains: `biology/`, `methods/`, `findings/`, plus `literature/`, `protocols/`, `decisions/`).

`agents/core/wiki.py` reads that wiki recursively and files new agent-written cards under the matching
domain subfolder. The generated card list is `wiki/index-cards.md`; the curated index is `wiki/index.md`.

Nothing to see here — start at [`../../wiki/index.md`](../../wiki/index.md).
