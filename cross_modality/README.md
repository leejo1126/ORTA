# cross_modality module

**Status:** planned
**One-liner:** registration + joint analysis across modalities (IF ↔ RNA ↔ DNA).

Planned sibling module under the ORTA umbrella. Its experiments reference runs from other modules by their
stable experiment IDs (via `../EXPERIMENTS.md`) — e.g. joint analysis of `IF_2026-04-16_v1` with an RNA run.
When activated, copy `_templates/module/` structure. Register in `../OVERVIEW.md`.
