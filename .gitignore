# Data & outputs (large, regenerable)
data/
*.zarr/
*.h5ad
*.tif
*.tiff
*.png
*.npz
*.npy

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.ipynb_checkpoints/

# Snakemake
.snakemake/

# Secrets (never commit API keys)
.env
*.key
.secrets/
agents/.anthropic_key

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# agent lab notebook: keep the structured text record (stats/verdict JSON,
# HISTORY.md, history.jsonl) under version control as the tuning audit trail.
# Montages stay out of git (large, regenerable) — they live in IF/data/figures/
# and are referenced by path from the notebook records.
