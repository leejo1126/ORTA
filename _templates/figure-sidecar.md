<!-- Optional per-figure sidecar for a keeper that needs more than one CATALOG row.
     Name it results/figures/<name>.md next to <name>.png. -->
# <figure filename>

- **What it is:** <one line>
- **How generated:** `<cmd/script>` · experiment `<ID>` · config `config.snapshot.yaml` · commit `<sha>`
- **How to read:** <panels, axes, color meaning, units, n>
- **What it says:** <the result / interpretation>
- **Caveats:** <thresholds, exclusions (e.g. beads), stats>
- **Links:** `[[wiki-page]]` · related figures
